package com.foobnix.activity.files;


public class FolderDialogs {
    /*
     * public void createFolderDialog(Context context) { final EditText name =
     * new EditText(context); new AlertDialog.Builder(context)//
     * .setView(name)// .setTitle("Create Directory")//
     * .setPositiveButton("Create", new DialogInterface.OnClickListener() {
     * public void onClick(DialogInterface dialog, int whichButton) { String
     * text = name.getText().toString(); if (StringUtils.isEmpty(text)) { File
     * file = new File(currentPath, text);
     * 
     * LOG.d("Create file", file.getPath()); if (file.mkdir()) {
     * adapter.notifyDataSetChanged(); } else {
     * Toast.makeText(getApplicationContext(), "Can't create directory",
     * Toast.LENGTH_LONG).show(); } }
     * 
     * } })// .setNegativeButton("Cancel", new DialogInterface.OnClickListener()
     * { public void onClick(DialogInterface dialog, int whichButton) {
     * dialog.cancel(); } }).show();
     * 
     * }
     */

}
